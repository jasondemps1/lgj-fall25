(in-package :lgj-fall25)

;; COMPONENTS

;;(defcomponent image
;;  "Image Component"
;;  (surface (cffi:null-pointer) :type cffi:foreign-pointer)
;;  (width 0 :type fixnum)
;;  (height 0 :type fixnum)
;;  (scale 1.0 :type single-float))

;; SYSTEMS

;;(defsystem render-system
;;"Render images based on position"
;;(:components-ro (position image)
;;:components-rw (renderer))
;;(let ((scaled-width (* image-scale image-width))
;;(scaled-height (* image-scale image-height)))
;;(sdl2:render-copy-ex)))

